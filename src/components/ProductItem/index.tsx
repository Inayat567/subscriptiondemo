import {View, Text} from 'react-native';
import React from 'react';
import {PIS} from './ProductItem.styles';

const ProductItem = () => {
  return (
    <View style={PIS.container}>
      <Text>ProductItem</Text>
    </View>
  );
};

export default ProductItem;
